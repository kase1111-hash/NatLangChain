# NatLangChain Dependencies
# For development: pip install -e ".[dev]"
# For production: pip install -r requirements.txt

# Core dependencies (pinned for reproducibility)
flask==3.0.0
anthropic==0.34.2
python-dotenv==1.0.0
sentence-transformers>=2.2.0
numpy>=1.20.0
cryptography>=41.0.0
