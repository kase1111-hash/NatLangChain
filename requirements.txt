# NatLangChain Dependencies
# =============================================================================
#
# Core dependencies only. For optional extras, see pyproject.toml:
#   pip install ".[dev]"       — Development (pytest, ruff, mypy)
#   pip install ".[postgres]"  — PostgreSQL storage backend
#   pip install ".[production]"— Production deployment (gunicorn)
#
# =============================================================================

# Core
flask>=3.0.0
anthropic>=0.34.2
python-dotenv>=1.0.0

# Semantic search (embeddings + similarity)
sentence-transformers>=2.2.0
numpy>=1.20.0

# Encryption at rest
cryptography>=41.0.0

# LLM provider HTTP clients (Ollama, llama.cpp)
requests>=2.28.0
