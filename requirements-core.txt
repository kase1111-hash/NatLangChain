# Core dependencies for NatLangChain (minimal install)
# This creates a lightweight backend without ML features

flask==3.0.0
python-dotenv==1.0.0
werkzeug>=3.0.0
cryptography>=41.0.0  # For data encryption at rest

# Optional: Install full requirements.txt for semantic search and LLM features
